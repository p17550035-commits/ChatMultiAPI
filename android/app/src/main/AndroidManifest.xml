<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:label="ChatMultiAPI"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- Crash Log Viewer Activity -->
        <activity
            android:name=".CrashLogActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- Main Launcher Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>

<!-- ========================================================================
     METADATA FOOTER — AndroidManifest.xml
     version: 1.0.3
     local_timestamp: 07/07/2026 04:19 AM EDT
     utc_timestamp: 2026-07-07T08:19:00Z

     ML TAGS
     - ml_tags: ["manifest", "theme_fix", "appcompat_required"]

     SECTION PURPOSE
     - Applies a valid AppCompat theme required by AppCompatActivity.
     - Prevents the IllegalStateException pre-launch crash.
     - Ensures MainActivity and CrashLogActivity can initialize normally.

     NOTES
     - No other modules or resources changed.
     - AppCompat theme is mandatory for your activity base class.
     ========================================================================
     END OF FILE :: CHATMULTIAPI :: GODMODE
-->
