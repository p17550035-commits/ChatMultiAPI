<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:label="ChatMultiAPI"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/AppTheme">

        <!-- Crash Log Viewer Activity -->
        <activity
            android:name=".CrashLogActivity"
            android:exported="false"
            android:theme="@style/AppTheme" />

        <!-- Main Launcher Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/AppTheme">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>

<!-- ========================================================================
     METADATA FOOTER — AndroidManifest.xml
     version: 1.0.2
     local_timestamp: 07/06/2026 08:41 PM EDT
     utc_timestamp: 2026-07-07T00:41:00Z

     ML TAGS
     - ml_tags: ["manifest", "crash_logger", "activity_registration", "theme_fix"]

     SECTION PURPOSE
     - Registers CrashLogActivity for global crash diagnostics.
     - Maintains MainActivity as launcher.
     - Adds required AppTheme to prevent pre-launch crash.

     NOTES
     - AppTheme is minimal and safe.
     - No additional modules required.
     ========================================================================
     END OF FILE :: CHATMULTIAPI :: GODMODE
-->
