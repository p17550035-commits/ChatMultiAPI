plugins {
    id 'com.android.application' apply false
    id 'org.jetbrains.kotlin.android' apply false
    id 'com.chaquopy.python' version'17.0.0' apply false
}

android {
    namespace 'com.example.chatmultiapi'
    compileSdk 34

    defaultConfig {
        applicationId "com.example.chatmultiapi"
        minSdk 24
        targetSdk 34
        versionCode 1
        versionName "1.0"

        python {
            version "3.10"
        }

        ndk {
            abiFilters "armeabi-v7a", "arm64-v8a"
        }
    }

    sourceSets {
        main {
            python.srcDir "src/main/python"
        }
    }
}

chaquopy {
    defaultConfig {
        pip {
            install "kivy"
            install "requests"
            install "plyer"
        }
    }
}
